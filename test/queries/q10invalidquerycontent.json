{
    "title": "SELECT dept, id, avg WHERE avg != 95 && avg > 90 && dept == 'cpsc' ORDER BY avg",
    "query": { "wrong stuff":"asdfsdf"

    },
    "response": {
        "code": 400,
        "body": {
            "error":"Query format is wrong"
        }
        }
}
